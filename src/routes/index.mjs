/*import express from "express";
const router = express.Router();

// Index SIN layout
router.get("/", (req, res) => {
  res.render("index", { title: "Ingreso Principal" });
});

// Landing de países CON layout
router.get("/paises", (req, res) => {
  res.render("landing", { title: "Inicio Países" });
});

// Dashboard (lista de países)
router.get("/dashboard", async (req, res) => {
  const paises = []; // cargar de DB
  res.render("dashboard", { title: "Listado de Países", paises });
});

export default router;*/

